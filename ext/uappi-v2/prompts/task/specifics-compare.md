Comparar específicos:

Versão alvo do core (tag):

Contexto opcional:
- Repo core GitLab:
- Escopo (subpasta de especifico/):
- Caminho do repo cliente (se o cwd não for o cliente):

A versão instalada no cliente (`.wapstore/build`) é só referência no relatório. A comparação usa **somente** a tag informada em "Versão alvo" — sem ela o fluxo não roda.
