Comparar específicos:

Contexto opcional (preencha só o que quiser sobrescrever):
- Release do core (tag):
- Repo core GitLab:
- Escopo (subpasta de especifico/):
- Caminho do repo cliente (se o cwd não for o cliente):
